<Window x:Class="FixBinaries.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="FixBinaries - Configure ZEMAX Paths for OpticStudioMCPServer"
        Width="620" Height="460"
        MinWidth="500" MinHeight="400"
        WindowStartupLocation="CenterScreen"
        Loaded="Window_Loaded">
    <Window.Resources>
        <Style x:Key="DllIndicator" TargetType="TextBlock">
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Margin" Value="0,2,16,0"/>
        </Style>
    </Window.Resources>

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- OpticStudio Install Folder -->
        <TextBlock Grid.Row="0" Text="OpticStudio Install Folder:" FontWeight="SemiBold" Margin="0,0,0,4"/>

        <Grid Grid.Row="1" Margin="0,0,0,4">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBox x:Name="txtOpticStudioPath" Grid.Column="0" VerticalContentAlignment="Center"
                     IsReadOnly="True" Padding="4,3"/>
            <Button Grid.Column="1" Content="Browse..." Padding="12,3" Margin="8,0,0,0"
                    Click="BrowseOpticStudio_Click"/>
        </Grid>

        <WrapPanel Grid.Row="2" Margin="0,0,0,12">
            <TextBlock x:Name="txtZosapi" Style="{StaticResource DllIndicator}"/>
            <TextBlock x:Name="txtZosapiInterfaces" Style="{StaticResource DllIndicator}"/>
            <TextBlock x:Name="txtNetHelper" Style="{StaticResource DllIndicator}"/>
        </WrapPanel>

        <!-- Detected Installations -->
        <DockPanel Grid.Row="3" Margin="0,0,0,12">
            <TextBlock DockPanel.Dock="Top" Text="Detected Installations:" FontWeight="SemiBold" Margin="0,0,0,4"/>
            <ListBox x:Name="lstInstallations" SelectionChanged="Installations_SelectionChanged"/>
        </DockPanel>

        <!-- Status -->
        <TextBlock Grid.Row="4" x:Name="txtStatus" Text="Ready" Foreground="Gray" Margin="0,0,0,8"/>

        <!-- Generate Button -->
        <Button Grid.Row="5" x:Name="btnGenerate" Content="Generate ZemaxPaths.props"
                HorizontalAlignment="Right" Padding="16,6"
                IsEnabled="False" Click="Generate_Click"/>
    </Grid>
</Window>
